
const Layout = ( { children } ) => {
    return (
        <div className="flex flex-col items-center mt-20 text-cyan-600 font-bold">
            { children }
        </div>
    )
}

export default Layout